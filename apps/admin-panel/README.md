curl --request POST \
  --url  \
  --header 'content-type: application/json' \
  --data '{
    "client_id": ,
    "client_secret": ,
    "audience": ,
    "grant_type": "client_credentials"
  }'


Generates new access token